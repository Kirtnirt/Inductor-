const int emfPin = 35; // Analog pin
const int threshold = 100; 

void setup() {
  Serial.begin(115200);
}

void loop() {
  int emfValue = analogRead(emfPin);
  if (emfValue > threshold) {
    Serial.print("Back EMF: ");
    Serial.println(emfValue);
  }
  delay(100);
}
